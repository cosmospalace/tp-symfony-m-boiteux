<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_pilote') }}">TP1 - TB</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
        data-bs-target="#navbarColor01" aria-controls="navbarColor01" ariaexpanded="
        false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link dropdown" href="{{ path('app_pilote') }}">Pilotes</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_qualification') }}">Qualifications</a>
                </li>
            </ul>
        </div>
    </div>
</nav>